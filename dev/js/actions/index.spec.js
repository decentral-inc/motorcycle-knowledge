import { expect } from '../testhelper';
import { selectUser } from '.';

describe('actions', () => {
  describe('selectUser', () => {
    it('has the correct type', () => {
      expect(3).to.equal(3);
    });
  });
});
